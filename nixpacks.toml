[phases.setup]
nixPkgs = [
  "nodejs_20",
  "npm",
  "ffmpeg",
  "python312",
  "python312Packages.pip",
  "tesseract",
  "gcc",
  "pkg-config",
  "wget",
  "ca-certificates"
]

[phases.install]
cmds = [
  "python3 --version",
  "pip install yt-dlp spotdl gallery-dl --break-system-packages",
  "npm ci"
]